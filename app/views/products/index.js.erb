function replaceProducts (innerHTML) {
  const searchResults = document.querySelector('.search-results-container');
  searchResults.innerHTML = innerHTML;
}
replaceProducts("<%= j render 'products', products: @products %>");


